{
  "scripts": {
    "build": "tailwindcss -i ./src/style.css -o ./docs/style.css --minify",
    "watch": "tailwindcss -i ./src/style.css -o ./docs/style.css --minify --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.3"
  }
}
